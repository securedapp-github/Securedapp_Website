import React from "react";

const DescriptionFor = ({ content }) => {
  return (
    <div className="text-customColor pt-5 mt-[0px] text-[16px] font-normal font-sans leading-[135%] text-opacity-80">
      {content}
    </div>
  );
};

export default DescriptionFor;
